# Store Page Templates

Use `StorePageShell` for store-owned pages and views that need the quiet store
navigation surface: product detail, cart, orders, and similar store workflows.

The shell owns the top-left back affordance and optional page heading/actions.
Keep product-specific hero layouts, checkout tools, and order tables inside the
shell instead of reimplementing a competing page container.
