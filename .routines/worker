# Routine: worker

## Purpose

A senior fullstack execution routine for turning an existing `.todo` entry into tested, documented repository changes. Use this routine when implementation is requested or when a `.tip` next step is ready to execute.

## Triggers

- User references `.routines/worker`
- User asks to execute, continue, finish, or validate an existing `.todo` entry
- A `.tip` file has concrete next steps that require code, tests, migration, or docs changes

## Inputs

- `.todo/<entry>` and `.todo/<entry>.tip` for the requested work
- `.todo/README.md` for todo/status conventions
- Relevant `.specs/*` files for affected architecture, models, or protocols
- `.specs/security_architecture_spec` for every security-sensitive change
- `.routines/auditor` for backend/frontend/security/UI quality standards
- `.routines/correctness` for drift and contradiction checks
- `.routines/planner` when specs or model/protocol docs may change

## Responsibilities

- Review the targeted `.todo` entry, its `.tip` status, and current project state
- Honor `.tip` `@todo-sequence` declarations: do not start a higher indexed plan
  while a lower member in the same group remains incomplete unless the
  coordinator tip explicitly records an exception
- Analyze and revise plans for accuracy, feasibility, and alignment with project standards
- Execute the plan, updating the entry's `.tip` as work progresses
- Maintain a detailed, replicable checklist for non-trivial work
- Prioritize backend modeling, extension, and polish before frontend work if both are required
- Enforce idiomatic Go backend layering: service, interface, repository, API, caching, models, and tests
- Enforce centralized, service-level, fail-closed policy gates; never ship a
  sensitive action protected only by handlers, middleware, claims, IP, or UI
- Keep implementations reliable, maintainable, and testable
- Add or update backend/frontend tests proportional to risk and blast radius
- Reference `.specs/` and `.routines/planner` for authoritative architecture and model decisions

## Non-Goals

- Do not invent unrelated todos; use `.routines/planner`
- Do not perform broad audits unrelated to the active todo; use `.routines/auditor` or `.routines/correctness`
- Do not mark a `.tip` complete without relevant verification
- Do not duplicate spec content inside todo files; link to specs and summarize only the delta

## Goal

Deliver robust, high-quality, spec-compliant changes by executing todos with continuous plan revision, verification, and traceability.

## Principles

- Backend-first when both backend and frontend are involved
- Prefer existing repo patterns and established libraries over custom mechanisms
- Keep work tightly scoped to the todo and related contracts
- Link to specs/routines instead of copying long context
- Treat `.tip` as the current status record for implementation work
- Update specs immediately after architecture, model, migration, or protocol changes

## Workflow

1. Identify the target `.todo` entry from the user request or active `.tip`
2. Read the target plan, target `.tip`, relevant specs, and `.routines/auditor` when quality gates apply
3. If `@todo-sequence` is present, resolve the group and confirm all lower member
   indexes are complete or the coordinator documents an explicit exception
4. Compare the plan against current code and revise the working checklist if needed
5. Implement backend work first when shared contracts or data models are involved
6. Implement frontend work after backend contracts are modeled and tested
7. Run verification proportional to the change's blast radius
8. Update `.tip` progress and any affected specs/routine run remarks
9. Do a final diff/status review and report changed files plus verification results

### Pseudocode

```
todo = selected .todo entry
read(todo, todo.tip, relevant specs, relevant audit routines)
revise checklist against current code
if backend work:
    apply auditor backend/security/data standards
    implement with focused tests
if frontend work:
    apply auditor UI/state/API standards
    implement with focused tests/build
run verification proportional to blast radius
update specs and .tip with durable facts
```

## Verification

- Backend changes: from `backend/`, run `JWT_SECRET=test-secret go test ./...` unless a narrower targeted run is explicitly justified in the `.tip`
- Frontend changes: from `frontend/`, run build/test/lint commands relevant to the touched area
- Migrations: verify fresh-schema and legacy/idempotency behavior where possible; record DB-backed coverage gaps when `TEST_DATABASE_URL` is unavailable
- Shared contracts: run both backend and frontend verification

## Trace Outputs

- Update `.todo/<entry>.tip` with phase status, completed checks, and remaining next steps
- Update `.todo/<entry>` when the plan itself changed
- Update `.specs/*` through `.routines/planner` when architecture, models, protocols, migrations, or frontend contracts changed
- Note verification commands and any skipped tests with reasons

## Contexts

- `.todo/`
- `.todo/*.tip`
- `.routines/planner`
- `.routines/auditor`
- `.specs/`
- `backend/`
- `frontend/`

## Extension/See Also

- `.routines/planner` - planning, todo lifecycle management, and specs upkeep
- `.routines/auditor` - whole-repo quality, security, backend/frontend, and UI audit standards
- `.routines/correctness` - drift and anti-pattern checks
- `.specs/` - authoritative project specifications

## Last Ran

- 2026-08-23

## Last Run Remarks

- Adopted the documented `.tip` `@todo-sequence` API. Workers now resolve the
  declared group and refuse higher indexed work while a lower member remains
  incomplete unless the index-0 coordinator records an explicit exception.

- Began `db_transactions_concurrency` with an implementation-truth inventory:
  retained Skaia's existing transaction runner, atomic order/item creation,
  row-locked conditional stock reservation, advisory-locked wallet debits, and
  transaction-coupled reference payout credits.
- Added migration 038 and the fresh-schema equivalent for scoped hashed wallet
  operation identity, payload-conflict rejection, replay uniqueness, and
  forward-enforced positive amount/known type constraints. Raw operation keys
  do not persist or enter public models.
- Routed stock refunds and product wallet fulfilments through the idempotent
  ledger path, made malformed/unsupported fulfilments fail closed, and moved
  order completion after successful effects. Focused tests, race compilation,
  vet, migration parity, diff checks, and the complete backend suite pass;
  PostgreSQL concurrency execution remains pending a configured test database.

- Completed `section_picker_menu_revamp`: replaced the narrow absolute section
  dropdown with a responsive accessible dialog, complete category/type
  descriptions, description-aware search, list/grid views, token-based icon
  families, and preserved insertion/clipboard semantics.
- Verification passed: all 231 frontend tests, lint, TypeScript/production
  build, native-dialog, managed-form, loading-boundary, media-placeholder, todo
  pairing, spec-reference, and diff checks.
- Visual follow-up aligned the picker search, close, and view controls with the
  platform's shared compact FilterBar, Button, and directory-toggle patterns;
  the complete 231-test and frontend gate set passed again.

- Completed the resource-embed presentation correction: product pages and
  embeds now share one all-media carousel/lightbox owner, while documentation
  embeds use the real shared documentation shell in a bounded, internally
  scrollable embedded mode with the first authorized guide payload.
- Verification passed: all 227 frontend tests, lint, TypeScript, production
  build, media/native-dialog/loading/form policies, spec references, and diff
  checks.

- Completed the page-section presentation follow-up: new sections no longer
  persist picker labels as headings, legacy generated labels are viewer-hidden,
  empty heading wrappers reserve no space, and builder identification remains
  editor-only. All 226 frontend tests, lint, build, policy and diff gates pass.

- Completed the simplified `page_section_media_preview_and_resource_embeds`:
  every eligible `MediaPlaceholder` directly opens the shared accessible
  lightbox (including gallery images), and the 20-type registry has one compact
  `resource_embed` with resource-type/resource dropdowns for products, forum
  threads, and documentation.
- Verification passed: complete backend suite; all 221 frontend tests; lint,
  TypeScript, media/native-dialog/loading/form policies, and production build.

- Completed `guest_and_provisional_account_containment`: new accounts now use a
  server-owned 30-minute-or-TOTP trust decision, a closed deny-before-work HTTP
  registry, DB-backed sensitive WS policy, Redis mutation/upgrade/chat budgets,
  and optional-TOTP countdown onboarding. Removed guest checkout, wallet credit,
  full private page listing, raw visitor analytics, browser Grengo control and
  guest media/history mutation; public profiles are explicit DTOs.
- Verification passed: complete backend suite; WS/security race tests; all 213
  frontend tests; lint, TypeScript, native-dialog policy, production build,
  reference checks and diff checks.

- Completed Phase 2 and the intent/data portion of Phase 3 for
  `viewport_deferred_rendering_and_suspense`: custom-page browse is bounded,
  content-free, cursor-paginated, and batch-enriched; exact previews are
  service-authorized, sanitized, no-store, revision-validated, abortable,
  intent-only, inert, and held in a 24-entry client LRU. Complete backend and
  frontend suites, lint, build, TypeScript, media, and dialog gates pass.
- Completed the remainder of Phase 3 and Phase 4: exact previews now scope
  section observation to their clipped thumbnail, and shared frames defer every
  later view/editor renderer behind bounded type-shaped skeletons. First-section
  eagerness, editor intent, sticky draft mounts, collapse lifecycle, observer
  fallback, and page/JSON-ID teardown semantics have focused coverage.
- Corrected the Phase 4 visual fallbacks after review: restored the established
  responsive full-bleed hero silhouette and replaced generic section-family
  cards with renderer-specific text, icon, media, profile, CTA, code, table, and
  form compositions. All 205 frontend tests, lint, build, and policy gates pass.
- Closed Phases 5-7 with a single route-aware shell, shared primary-surface
  skeletons, interaction-only analytics/Recharts loading, and a focused loading
  policy. The bounded 500-record fixture emits 100 summaries/19,392 bytes with
  one repository call; complete backend and 210-test frontend suites pass.

## Previous Run Remarks (2026-08-17)

- Completed `multi_documentation_hub_and_forum_view`: added an independent
  lifecycle-aware multi-documentation backend and `/doc` authoring experience,
  a shared TipTap/sanitized-renderer boundary, and a `/forum/docs` presentation
  adapter that keeps forum records authoritative.
- Added fail-closed visibility/ownership and WebSocket subscription policy,
  content-free invalidation, revision conflicts, responsive accessible
  navigation, public SEO/sitemap discovery, forum projection tests, and compact
  durable documentation contracts. The complete backend suite, all 175 frontend
  tests, lint, native-dialog enforcement, production build, scoped root Go
  suite, separation/CSS/reference checks, todo pairing, and diff checks pass.
- Corrected the localhost rollout after live verification found two integration
  gaps: the frontend feature normalizer omitted `docs`, and `useBlocker` cannot
  run under Skaia's declarative `BrowserRouter`. The home tenant now declares
  `docs`; the replacement Prompt-backed link guard has focused coverage, and
  live documentation page/API probes return HTTP 200.
- Reworked the documentation UI after real-browser screenshots exposed an
  overflowing search label and unbounded generic forms. Catalog, sidebar
  management, settings, and both guide editors now use deliberate responsive
  hierarchy, visible labels/placeholders, route previews, compact actions, and
  token-only styling; the verified frontend was shipped to `home-backend`.
- Normalized the documentation module around Skaia's shared directory contract:
  `/doc` now uses FilterBar search, persisted grid/list modes, and TableView,
  while creation, settings/section management, and guide authoring live on
  dedicated routes. Forum documentation edits reuse the existing thread editor;
  text policy, focused Go tests, all 176 frontend tests, lint, native-dialog
  enforcement, TypeScript, and the production build pass. Management deep links
  are noindex SPA routes resolved before public doc slugs, and the rebuilt local
  tenant returns HTTP 200 for catalog, creation, settings, guide creation, and
  forum-documentation routes.
- Aligned documentation guide creation/editing with the shared New Thread/New
  Product routed-form surface. The bounded modal now owns header X/check actions,
  form-associated submission, loading/disabled feedback, shared field help, and
  a non-duplicated action hierarchy; all 49 frontend files and 177 tests, lint,
  native-dialog enforcement, and production build pass.

## Previous Run Remarks (2026-08-16)

- Completed `custom_page_document_model_consolidation`: restored one
  `pages.content` read/save/interactive authority, retained document validation,
  registries, renderers, shared UI, privacy, and locked response handling, and
  removed the unused typed/shadow/palette/generator/command implementation.
- Added a fail-closed normalized response/palette guard before schema retirement.
  All 172 frontend tests, lint, and production build pass; focused backend tests
  pass. The broad backend run reached all packages until the sandbox prohibited
  an existing `httptest` TCP listener in `internal/middleware`.
- Follow-up dead-code cleanup removed empty command/schema/frontend/source
  directories, unused registry exports, and stale README commands for the
  long-retired `backend/frontend` tree; focused backend and frontend gates still
  pass.

## Previous Run Remarks (2026-08-16)

- Completed all phases of `shared_media_placeholder_adoption`: hardened the
  shared image/video/audio state and layout contract, migrated first-party page
  content, rich text, uploads/inbox/previews, and commerce without changing stored
  HTML, editable TipTap documents, navigation, purchase actions, or live streams.
- Added validated repeated/cover backgrounds, migrated profile/security/branding/
  thumbnail surfaces, and reduced enforcement to reviewed native owners only.
  Focused tests, all 181 frontend tests, lint, TypeScript, production build,
  media/native-dialog enforcement, todo pairing, and diff checks pass.

## Previous Run Remarks (2026-08-03)

- Completed `forum_category_mobile_layout`: the category thread header no longer
  lets its non-shrinking search controls compress the category identity on real
  narrow mobile viewports. The identity and controls now occupy separate mobile
  rows, long text wraps safely, and the optional forum directory starts collapsed
  without a timed layout shift. All 155 frontend tests, lint, TypeScript, build,
  and diff checks pass.
- Completed `page_builder_editor_responsive_controls`: the Appearance editor now
  uses shared Select controls with working nested portals and visible-trigger
  focus, while TipTap drag controls survive global button styles on desktop and
  editable content uses compact renderer-like padding without drag controls on
  phones. Focused tests, full lint, TypeScript, and production build pass.
- Completed `repository_hygiene_and_text_policy`: added the dependency-free Go
  tracked-text checker and tests, emoji path allowlist, safe formatting repair,
  and a read-only POSIX Husky hook covering every Go module and frontend gate.
- Removed proven build/runtime artifacts, unreachable declarations and router
  wiring, inert commented code, unused frontend dependencies, policy violations,
  and orphaned todo material. Full hook, test, vet, staticcheck, corpus,
  generated-contract, and diff verification passes.

## Previous Run Remarks (2026-08-02)

- Completed the blocking `multi_tenant_seo_origin_integrity` regression pass:
  retryable per-response `INDEX_FILE_PATH` loading with hot-ship replacement,
  uncached 503/noindex dependency failures, typed authoritative/absence/degraded/
  error resolution, v3 authoritative-only metadata caching, best-effort Redis
  writes, and restored root `backends/` Docker exclusion.
- Added deterministic handler, SQL/config failure, cache, hot-ship, Docker-context,
  and race coverage; updated the backend, cache/CDN, infrastructure, security,
  and frontend-path contracts. Focused/full tests, race/static analysis, vet,
  builds, frontend production build, and diff checks pass. Production rollout
  and recrawl verification remain external to this checkout.
- The live local rollout exposed and repaired two additional legacy-state gaps:
  existing `/frontend/dist/index.html` tenant env values now migrate to `/app`,
  and default-server `444` handling no longer preempts the explicit IPv4 nginx
  healthcheck. The `home` backend and nginx were recreated and verified healthy
  with a raw `200` application response.

## Previous Run Remarks (2026-07-30T17:18:02-04:00)

- Implemented `multi_tenant_seo_origin_integrity` through the deployable code
  boundary: deterministic tenant canonical origins, origin-independent semantic
  metadata, versioned exact invalidation, single-context escaping, real
  404/noindex behavior, and side-effect-free image metadata.
- Hardened generated nginx with a local `/healthz`, fail-closed unknown hosts,
  restricted provisioned-site routing, canonical env generation/migration, and a
  generated two-tenant regression. Updated the governing specs and todo status.
- Full backend/root tests and vet, focused race/static analysis, builds, and diff
  checks pass. Production deployment and Cloudflare inventory remain explicitly
  tracked because the affected tenant configs are not present in this checkout.

## Previous Run Remarks (2026-07-30T13:05:28-04:00)

- Completed `bible_voice_reader_integration` with a startup-validated,
  internally embedded 66-book KJV corpus, public catalog/per-book APIs, and no
  database or remote scripture dependency.
- Added the public Bible browser and responsive reader, query/path deep-link
  preservation, bounded preferences and book caching, favorites/download,
  accessible dialogs, browser narration, continuous advancement, voice fallback,
  cancellation lifecycle, and keyboard controls.
- Follow-up polish moved book discovery/picking to `DirectoryLayout` with
  metadata search, division filtering, persisted grid/TableView modes, added
  viewport fullscreen reader behavior, and repaired the shared compact icon
  button sizing contract.
- Verification passed: all corpus checksums; complete backend suite; all 149
  frontend tests; contract drift; Biome lint; TypeScript; native-dialog guard;
  production build; dependency/color drift checks; and `git diff --check`.

## Previous Run Remarks (2026-07-26T07:59:09-04:00)

- Completed `websocket_ingress_trust_boundary_hardening`: client ingress is an
  explicit allowlist, private subscriptions are service-authorized and bounded,
  credential updates are direct-only, and recovery auth is bound to the exact
  guest session plus browser-tab request.
- The REST bridge is disabled by default and now has connection-bound identity,
  route/method/header validation, per-client/global concurrency limits,
  cancellation, deadlines, bounded response metadata/body, and safe teardown.
- Verification passed: full backend suite; WS/page race detector; all 124
  frontend tests; frontend lint, production build, and contract drift check.

## Previous Run Remarks (2026-07-18)

- Completed the final `typed_page_sections_redesign` UX hardening pass: the
  Appearance editor now uses a measured body portal with viewport collision
  handling and all five literal shell colors use the shared native precise
  picker while inherit/palette semantics remain intact.
- Added focused placement/accessibility/color tests, restored generated-contract
  reproducibility, repaired the four stale frontend baseline assertions, and
  finished with the complete backend suite plus frontend contracts, lint,
  TypeScript, production build, and all 119 frontend tests passing.
- The affected-scope production audit found no material code issue. Deployment
  remains distinct from normalized-read enablement or legacy retirement, which
  still require the production observation window and operator attestation.

- Completed `typed_page_sections_redesign` Phase 1 and started Phase 2 with the
  checked-in 19-type
  compatibility matrix, sanitized fixtures for all 12 production-observed page
  section types and three saved-preset types, canonical legacy mappings, and
  parse/save/copy/duplicate/shell/registry/live-payload regression tests. Made
  renderer dispatch exhaustive, restored the legacy `features` alias, and added
  a safe visible unknown-type fallback. Added four renderer-family DOM parity
  tests, embedded page-theme/shared-shell v1 JSON Schema contracts, a public
  registry contract endpoint, and a reusable fail-closed section-revision guard.
- Progressed Phase 2 with normalized item/preset and all 19 versioned config
  contracts. Registry definitions now derive version, defaults, capabilities,
  config schema, and optional item schema from the embedded canonical sources;
  drift and defensive-copy tests cover the public metadata. Canonical interactive
  config contracts intentionally exclude runtime records and result summaries.
- Added fail-closed normalized runtime validation with a locally cached Draft
  2020-12 validator, embedded-only reference resolution, schema-default checks,
  explicit safe CSS color validation, sanitized errors, and tests proving legacy
  config remains behind the compatibility boundary.
- Added deterministic backend-to-frontend contract generation for the section
  union, per-type config aliases, versions, defaults, and capabilities. Frontend
  `types.ts` now consumes the generated list; Go and npm checks fail on drift.
- Extended generation to normalized Go discriminators/config DTOs and added a
  bounded, depth-limited, duplicate-key-safe validation/decode factory with
  sanitized failures and default/type coverage for all 19 section types.
- Added the generic normalized frontend `PageSection<T>` boundary and a tested
  compatibility adapter that separates shared shell, generated per-type config,
  interactive runtime projections, and quarantine while retaining legacy keys
  and a reverse projection for the current document path.
- Closed `typed_page_sections_redesign` Phase 2 at the verified contract boundary.
  Renderer migration is Phase 3, shadow persistence is Phase 4, typed mutation
  and revision wiring is Phase 5, and cutover/rollback controls are Phase 7.
- Completed Phase 3 by moving all renderer families behind one `SectionFrame`
  and one shared toolbar, with six type-specific action portals, an exhaustive
  generated-metadata renderer registry, safe page-palette/color resolution,
  container width and overflow-safe content scaling, and accessible
  permission-aware collapse state. The compatibility adapter projects generated
  shell edits back into the legacy document without erasing unknown config.
- Verification passed: backend `go test ./...`; contract drift checks; 32 focused
  frontend tests; frontend TypeScript, lint, and build. The full frontend suite
  passes 109/113 tests; its only failures are the same four unrelated existing
  MFA/Header assertions recorded in the todo tip.
- Completed `typed_page_sections_redesign` Phase 4 with idempotent fresh and
  incremental normalized storage for page themes/tokens, section instances,
  palette references, items, presets, responses, quarantine, and parity runs.
  Ordinary page creates/updates now maintain the shadow transactionally while
  `pages.content` remains authoritative.
- Added bounded response-free legacy/normalized projectors, stable server-ID
  upserts, stale-row cleanup, safe quarantine/repair telemetry, palette
  referential integrity, and a resumable 1-500 page backfill command. Sensitive
  interactive records remain exclusively in the current document until Phase 6.
- Real PostgreSQL verification exposed and fixed existing baseline drift in the
  provisioning seed order, inbox participant migration, product-review table,
  store special-action columns, and role bootstrap password handling. A fresh
  database now runs all migrations twice successfully without rotating an
  existing database role credential.
- Verification passed: standard full backend suite; all Phase 4 pure and DB-backed
  tests; 11 focused frontend tests; contract drift check; frontend lint/build.
  A serial full DB-backed suite also passed the Phase 4 packages but exposed
  unrelated existing forum/store/user fixture and nullable-column failures,
  recorded in the todo tip.
- Completed `typed_page_sections_redesign` Phase 5 with a backend-owned,
  page-specific cutover gate; typed section CRUD/reorder and palette endpoints;
  centralized fail-closed page-editor policy; bounded generated config and shell
  validation; and sanitized content-free HTTP conflict responses.
- Added transactional page/row locking, expected section and theme revisions,
  stable normalized identities, independent-section convergence, revision-guarded
  structural changes, deferred theme-token ordering, and compatibility-document
  rebuilds that retain interactive records exclusively in the legacy runtime
  envelope.
- Moved the builder to per-section typed writes on eligible pages, with queued-edit
  flushing before structural mutations, explicit conflict recovery, typed
  numeric/string legacy-key identity, subscriber-targeted content-free page
  invalidations, replayed shared WebSocket subscriptions, and reconnect refetch.
- Verification passed: complete backend suite; focused serial PostgreSQL typed
  concurrency/theme/migration tests; frontend contract drift, lint, and production
  build; and all page-section tests. The full frontend suite passes 112/116 tests;
  only the same four unrelated MFA/Header baseline assertions fail.
- Completed `typed_page_sections_redesign` Phase 6 with migration 032, a bounded
  resumable sensitive-response backfill, page-scoped definition/response parity
  gates, and `PAGE_NORMALIZED_INTERACTIVE_RESPONSES`. Matched pages now use locked
  normalized response authority and transactionally refresh their rollback
  document; non-migrated pages retain the legacy response path.
- Preserved response keys, stable respondent identity, answers, statuses,
  moderator answers, pinned state, timestamps, and one-way idempotency parity.
  Raw idempotency keys are replaced in rollback projections, while both keys and
  hashes are explicitly excluded from participant/manager REST projections,
  result summaries, telemetry, audit metadata, logs, and WebSockets. Malformed
  interactive documents now sanitize to an empty result instead of failing open.
- Introduced `SectionPreset`, `preset_type`, and `/config/section-presets` as the
  preferred datasource-preset vocabulary while retaining the legacy model,
  discriminator, and route aliases. Preset writes now require a service-level,
  fail-closed `home.manage` policy.
- Verification passed: complete backend suite; serial PostgreSQL migration rerun,
  parity/idempotency/concurrency/moderation/type-race/disclosure tests; 20 focused
  frontend tests; contract drift; lint; TypeScript; and production build. The
  full frontend suite remains at 112/116, with only the same four unrelated
  MFA/Header baseline assertions failing.
- Completed `typed_page_sections_redesign` Phase 7 with migration 033,
  `PAGE_NORMALIZED_SECTION_READS`, page-specific consecutive parity/window and
  rollback-drill gates, normalized-only read projection, compatibility-endpoint
  usage telemetry, and the bounded `page-section-cutover` operational command.
- Added a deliberately non-destructive legacy-write retirement preflight. It
  requires every page ready for a full release window, no recent compatibility
  client writes, empty dormant legacy tables, and explicit external client/job
  review; legacy projection writes remain intact until real deployment evidence
  satisfies those conditions.
- Phase 7 verification passed: complete backend suite; focused PostgreSQL
  migration/cutover/rollback/telemetry tests; the live preflight runbook refusal;
  frontend contract drift, 26 focused page-section tests, lint, TypeScript, and
  production build.

## Previous Run Remarks (2026-07-17T08:42:00-04:00)

- Completed `interactive_sections_audit_remediation` across backend privacy,
  centralized page policy, integrity validation, content-free realtime, CORS,
  frontend participant/manager UX, regression tests, and durable specs.
- Verification passed: backend `go test ./...`; local Postgres concurrent
  builder-save/submit test; 12 focused frontend tests; lint; TypeScript; and build.

## Previous Run Remarks (2026-06-20)

- Executed `grengo_backend_refactor` / `grengo_cli_refactor` critical fixes and provisioning/real-time badge improvements:
  - **Fixed Frappe deployment failure**: `frappe.go` `cmdFrappeProvision` was calling `services.NewCommandRunner.RunSelf("docker", ...)` which executed `grengo docker compose ...` instead of the Docker binary. Fixed to use `exec.Command("docker", "compose", "-f", composePath, "up", "-d")` directly.
  - **Fixed SQL injection**: `dbExists` in `docker.go` interpolated raw `dbName` into SQL. Added `sanitizeDBName()` that strips non-alphanumeric/dash/underscore chars before use.
  - **Fixed real-time badge updates**: `provisioning:status` was only broadcast to `admin.general` permission holders. Now also broadcasts via `BroadcastToSubscribers("provisioning_logs", instanceID)` so ordinary users see live status changes.
  - **Added provisioning WS message type constants**: `ProvisioningProgress` and `ProvisioningStatus` added to `ws/message.go`.
  - **Frontend auto-subscribe**: `DeploymentsPage.tsx` now auto-subscribes to `provisioning_logs:{id}` for every loaded instance on mount, eliminating the need to expand a row to see live badge updates.
  - Removed unused `services` import from `frappe.go`.
- Build verified: `go build ./...` from `grengo/` ✅ and `backend/` ✅. `go test ./...` from `backend/` ✅ all pass.
- Grengo orchestration passcode/MFA: confirmed grengo's `apiPasscodeMiddleware` only enforces passcode if `.pcode` file exists and is non-empty. Grengo manages orchestration without passcode/MFA for machine-to-machine calls when `.pcode` is not configured.

## Next Steps

- Continue using `.todo/*.tip` as the entrypoint for implementation work.
