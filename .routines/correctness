# Routine: correctness

## Purpose

Architectural truth and drift-resolution routine for Go Web Platform. Use it to determine
what is actually correct when code, specs, routines, todos, migrations, tests, or
docs disagree.

## Triggers

- User references `.routines/correctness`
- Any routine asks for validation or a second pass
- Specs, todos, migrations, models, routes, or routines changed
- Documentation and implementation appear to disagree
- A finding needs authoritative classification before planning or implementation

## Inputs

- Changed files and git diff/status
- Relevant `.specs/*`, especially `.specs/README.md` and `.specs/PROTOCOL.md`
- `.specs/security_architecture_spec` when classifying security intent versus implemented truth
- Relevant `.todo/*` and `.todo/*.tip`
- Relevant `.routines/*`
- `backend/internal/migrations/`, `backend/models/`, route handlers, tests
- Frontend atoms/hooks/routes/components when UI/data contracts are involved

## Responsibilities

- Resolve contradictions between documented intent and implementation facts.
- Identify stale references, orphaned docs, duplicated routine authority, and invalid assumptions.
- Decide the professional path for each issue: patch docs, update specs, create/update todo, or implement through `worker`.
- Keep routines subject to correctness, including `auditor` and this file.
- Produce at least one actionable finding, or explicitly state that the requested scope had no material drift.

## Non-Goals

- Do not perform broad implementation work; use `.routines/worker`.
- Do not create large feature plans; use `.routines/planner`.
- Do not replace `.routines/auditor` for enterprise-grade quality review.

## Workflow

1. Define the contradiction/drift scope.
2. Read only the relevant specs, routines, todos, migrations, and source files.
3. Establish implementation truth from code/tests/migrations.
4. Compare docs/routines/todos against that truth.
5. Patch small safe drift immediately.
6. For larger work, record a precise finding and route it to `auditor`,
   `planner`, or `worker`.
7. Verify stale names/paths are gone and linked files exist.

## Verification

- Use `rg` for removed/renamed names, routes, files, and routine references.
- For docs-only corrections, verify linked files exist.
- For code-backed corrections, run the relevant tests/builds or record why not.

## Trace Outputs

- Updated docs/specs/routines/todos for safe drift fixes
- `.todo/<entry>.tip` notes for implementation-related findings
- Correctness run remarks for material standalone audits

## Contexts

- `.routines/`
- `.specs/`
- `.todo/`
- `main.go`
- `router.go`
- `internal/`
- `backend/`
- `frontend/`

## Extension/See Also

- `.routines/auditor` - stricter whole-repo quality gate
- `.routines/planner` - convert findings into plans and compact specs
- `.routines/worker` - execute scoped fixes

## Last Ran

- 2026-08-24

## Last Run Remarks

- Reconciled component-group editing with existing authority: presets continue
  to live in `custom_sections` and update through `/config/section-presets`, while
  ordering, widths, literal bindings, and icon positions remain config-only
  presentation state. No second datasource or page-document authority was added.

- Reconciled the public datasource cache contract with worker behavior: compile
  and execute now use one conveyor manager, deterministic ID/input flights, and
  a cache recheck by the winner rather than separately queued lock waiters.

- Reconciled datasource intent with implementation: editor previews no longer
  execute compiled code or inject env values in the browser; the protected
  backend runner owns unsaved and saved execution, and its VM/process boundary
  no longer exposes Node host constructors or inherited backend credentials.

- Reconciled `wipe all` with its advertised destructive contract. Compose-down
  errors were incorrectly terminating before client removal and per-client
  deletion lacked the established ownership fallback; both paths now converge
  on verified cleanup and cannot emit false completion.

- Reconciled CLI tenant defaults with backend feature truth. The former repeated
  eight-feature lists now derive from the same registry as the backend's default
  and optional projection; new/reset clients receive all twelve current modules.

- Reconciled publication discussions with existing forum authority: each
  proposal, showcase, and event now references one thread owned by its author;
  viewing/editing reuse canonical forum routes and policies, while community
  creation and lifecycle retain the cross-resource invariant.

- Reconciled interactive reuse with domain authority: `pages.content` remains the
  authority only for authored page responses, while proposal votes/transitions
  and event attendance reuse the interactive UI through a mutation binding and
  remain authoritative in community tables/services. No duplicate state remains.

- Reconciled community owner controls with the forum/store permission pattern:
  edit and delete-other authority use named extended permissions, workflow
  moderation remains `community.manage`, and `home.manage` applies only to the
  linked page. Shared comfortable-shell centering now has one CSS authority.

- Reconciled community publication content with custom-page authority: every
  publication now references one owned page, community reads use caller-safe page
  projections, and custom-page plus publication lifecycle moves together. No
  duplicate community document authority remains.

- Reconciled the new community/rewards/rankings route implementations with the
  established store depth-shell and custom-page document authorities. Shared
  module geometry now owns navigation and content width; community bodies reuse
  `pages.content` section semantics; legacy rich text remains readable; and
  empty reward provider collections are stable arrays. No material drift remains.

- Reconciled app discovery with the implemented route and feature contracts:
  `/community` is the unified public onboarding surface, while the shared drawer
  includes status, identities, rewards, rankings, proposals, showcases, and
  events under their existing visibility boundaries. No material drift remains.

- Reconciled migrations 042-044 and the fresh schema with rewards, rankings,
  community services/routes/UI, typed permissions, lifecycle providers, and
  durable specs. The full platform-gap sequence is complete and its verified
  todo pairs were retired; no material implementation/spec contradiction remains.

- Reconciled linked-identity migration 041, service routes, public DTO privacy,
  settings/profile UI, and sequence state. Opaque subjects remain private and
  Stage 4 is now the first incomplete platform-gap member.

- Reconciled health/status routes, migration 040 and fresh-schema parity,
  feature/SEO behavior, backup/release/load CLI contracts, operations evidence,
  and sequence state. The restored deployment and documented counts/readiness
  agree; the only remaining environment drift is the legacy superuser runtime
  login, now recorded in its owning soft-delete rollout handoff.

- Reconciled implementation with the proven custom-page authority. Runtime code
  now has no typed/shadow/palette branches or flags; `pages.content` owns page
  definitions and interactive records, and validation explicitly accepts the
  observed heterogeneous identity/config shapes without dropping unknown data.
- Confirmed the remaining renderer and component registries are descriptive
  allowlists rather than universal storage schemas. Migration 035 fails closed
  on the only two facts that would contradict safe retirement: normalized
  responses or page palette tokens.

## Previous Run Remarks (2026-08-16)

- Reconciled the typed-page completion claim with runtime and local-tenant truth.
  The browser still reads/saves `pages.content`; all normalized flags are absent
  and default off, but page creates/updates unconditionally maintain shadow rows.
  The `home` tenant also contains object/string configs, numeric/string IDs, the
  `features` alias, and all five interactive types, demonstrating that the strict
  normalized contract is not the actual page contract.
- Classified the professional correction as selective consolidation rather than
  a wholesale revert: retain registry/component metadata, JSON interactive
  safeguards, content-free realtime, renderer dispatch, and useful shared UI;
  remove the parallel normalized storage/API/generator/orchestration system only
  after a normal backup and lightweight checks for enabled flags, normalized-only
  responses, or palette tokens.
- Operator confirmation establishes that production never ran the normalized
  migration/backfill/cutover commands. This removes content migration from the
  correction path; only unexpected normalized response rows or palette tokens
  need a tenant-specific stop condition before schema cleanup.

## Previous Run Remarks (2026-08-16)

- Reconciled the shared-media plan with implementation truth. Corrected the
  hardcoded placeholder shadow, stale URL-event race, fixed-frame/composability
  gaps, and the mistaken treatment of semantic avatars as content media.
- Confirmed read-only rich text now sanitizes and adapts media without persisting
  transformed HTML, while editable TipTap serialization is unchanged. The
  reviewed exact-count gate preserves intentional avatar, bundled-art, and
  `MediaStream.srcObject` exceptions and exposes remaining phase debt.
- Found and corrected the shared preview dialog swallowing Escape from focused
  descendants. Upload, scraper, and commerce previews now share native modal,
  focus-restoration, keyboard-cycle, and failure-safe control behavior; completed
  Phase 3/4 files no longer appear in the migration allowlist.
- Replaced request/config-controlled URL interpolation in generated background CSS
  with a validated shared adapter, preserving cover and repeated-skin behavior
  while removing the injection boundary. Phase 5 consumers now agree with the
  durable media contract; only reviewed shared/avatar/bundled/MediaStream/Markdown
  owners remain native or allowlisted.

## Previous Run Remarks (2026-08-03)

- Reconciled repository hygiene policy with implementation: all tracked
  maintainable text now passes the Go checker, generated/vendor comments retain
  their source ownership, and fingerprinted Bible metadata was updated with its
  governed rendering hash while verse payload checksums remained unchanged.
- Removed only evidence-backed dead artifacts and declarations. Complete Go,
  frontend, static-analysis, contract, corpus, hook, and diff gates pass.

## Previous Run Remarks (2026-07-30)

- Reconciled the multi-tenant SEO implementation with the backend, caching/CDN,
  infrastructure, and security contracts. Canonical identity is configuration
  owned, rendered request origins are absent from Redis, and nginx health/unknown
  host traffic cannot warm a tenant route.
- Corrected two final drift points: static `privacy`/`tos` aliases and old page
  slugs are invalidated with page mutations, while mutable in-memory stream
  metadata is never cached. No material code/spec contradiction remains.
- Production verification is intentionally not marked complete: the affected
  tenant configs are absent locally and the public sites still serve the old
  metadata until deployment.

## Previous Run Remarks (2026-07-30)

- Reconciled the completed Bible implementation against backend, frontend,
  route-resolution, caching, security, todo, and routine contracts.
- Found and resolved the material cache-policy tension: immutable KJV responses
  still use the required API `no-store` headers, while reuse is limited to the
  embedded backend repository and a bounded eight-book browser memory cache.
- Confirmed invalid deep links fail visibly instead of leaking remembered
  coordinates, the corpus is not persisted or fetched remotely, and no
  Tailwind/Redux/Radix dependency crossed into the integration. Full tests,
  contract drift, lint, TypeScript, build, checksum, and diff checks pass.

## Previous Run Remarks (2026-07-30)

- Corrected the aggregate trash listing failure caused by manager-only
  `page_allocation` SQL skipping placeholder `$1` while the caller still passed
  four arguments. PostgreSQL therefore could not infer `$1` and returned
  `42P18`.
- Found and fixed the same latent drift in the manager-only `role` provider.
  Both providers now return an empty result before querying for ordinary users
  and use manager-only SQL with a matching argument list. Restore follows the
  same fail-closed boundary.
- Replaced temporary `fmt.Println` diagnostics with contextual server-side
  endpoint logging. Added provider-specific list/restore coverage and an
  aggregate PostgreSQL test that executes every provider registered by
  `main.go`; focused database tests and `go test ./...` pass.

## Previous Run Remarks (2026-07-26)

- Final WebSocket correctness sweep added the required `rows.Err()` terminal
  check in media history loading and split the duplicate subscription assertions
  that triggered `SA4000`. `staticcheck ./internal/ws/...`, `go vet
  ./internal/ws/...`, and the WS/page race tests now pass.
- Repository-wide staticcheck still reports pre-existing findings outside this
  scope across auth, config, datasource, email, grengo, integration, page,
  provisioning, section registry, and syslog; none are in `internal/ws`.
- Reconciled the WebSocket implementation and durable contracts: server-only
  message types are not client-originated, user credentials are direct-delivery,
  order sellers receive filtered direct projections, and normal clients no
  longer request denied order or other-user subscriptions.
- Removed lifecycle/protocol drift by making registration and subscriptions
  synchronous, idempotent, bounded, and disconnect-safe; updated realtime,
  security, and frontend specs. No material contradiction remains.

## Earlier Run Remarks (2026-07-17)

- Re-audited the post-reset interactive implementation and confirmed the governing
  architecture stores definitions and records in `pages.content`; retired the
  contradictory table/domain todo and routed behavioral gaps through the
  interactive-section remediation that is now preserved in durable specs.

## Next Steps

- Use correctness to validate routine/spec/todo contradictions before planning or implementation.
