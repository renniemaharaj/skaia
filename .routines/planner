# Routine: planner

## Purpose

Planning and durable-context upkeep routine for Skaia. Use it to create, split,
dedupe, update, or retire `.todo/` entries and to keep `.specs/` aligned with
implemented architecture, models, migrations, protocols, frontend contracts, and
operational policies.

## Triggers

- User references `.routines/planner` or asks to plan, strategize, organize, or retire work
- A todo lacks a `.tip` file or has stale status
- Multiple todos overlap or conflict
- Backend/frontend protocol, model, route, migration, WebSocket, cache, auth,
  infrastructure, or dynamic-page behavior changed
- `worker`, `auditor`, or `correctness` finds planning or spec drift

## Inputs

- `.todo/README.md`
- Relevant `.todo/*` and `.todo/*.tip` files
- `.specs/README.md`, `.specs/PROTOCOL.md`, and affected spec cards
- Changed code/tests/migrations when updating specs
- `.routines/correctness` for truth/drift questions
- `.routines/auditor` for quality gates or audit-driven plans
- `.routines/worker` when planning executable implementation steps

## Responsibilities

- Generate, organize, and maintain todos in `.todo/`.
- Ensure every active todo has a compact, current `.tip`.
- Split, merge, dedupe, or retire todos when it improves execution clarity.
- Keep todo entries scoped, actionable, and linked to relevant specs/routines.
- Keep `.specs/` authoritative, compact, and current after contract changes.
- Document durable architecture/model/protocol decisions without pasting full code.
- Route quality/security/UX findings through `.routines/auditor` when the right fix is unclear.
- Escalate contradictions to `.routines/correctness` before encoding them as plans or specs.

## Non-Goals

- Do not implement code changes; hand execution to `.routines/worker`.
- Do not run broad quality audits; use `.routines/auditor`.
- Do not decide contradictory facts by preference; use `.routines/correctness`.
- Do not copy large spec bodies into todos or large code/migration bodies into specs.

## Workflow

1. Identify whether the task is todo planning, spec upkeep, or both.
2. Read the smallest relevant todo/spec/routine set.
3. For todos:
   - Confirm matching `.tip` files exist.
   - Validate any `@todo-sequence` group has one basename-matching index-0
     coordinator and unique contiguous positive member indexes.
   - Compare plan/status with current project state when needed.
   - Update, split, merge, or retire with documented verification basis.
4. For specs:
   - Read affected spec cards and implementation truth.
   - Patch compact contracts using `.specs/PROTOCOL.md`.
   - Run reference checks for renamed routes/files/messages/tables.
5. Cross-link related specs, todos, and routines.
6. Record durable findings in `.tip`, specs, or planner run remarks.

## Verification

- Confirm every active todo has a matching `.tip`.
- Confirm new plans do not duplicate existing `.todo` entries.
- Confirm `.tip` files follow `.todo/README.md`.
- Confirm execution-sequence declarations have valid group names, one
  coordinator, and unique contiguous indexes.
- For retired todos, record verification basis before deletion.
- For specs, use `rg` to confirm stale names/paths/contracts are gone.
- Verify linked files exist.
- For code-backed spec changes, cite the verification already run by `worker` or `auditor`.

## Trace Outputs

- New or updated `.todo/<entry>` files
- New or updated `.todo/<entry>.tip` files
- Updated `.specs/*` files
- Planner run remarks for material planning/spec maintenance

## Contexts

- `.todo/`
- `.todo/*.tip`
- `.todo/README.md`
- `.specs/`
- `.specs/README.md`
- `.specs/PROTOCOL.md`
- `.routines/correctness`
- `.routines/auditor`
- `.routines/worker`
- `main.go`
- `router.go`
- `internal/`
- `backend/`
- `frontend/`

## Extension/See Also

- `.routines/worker` - execution of planned work
- `.routines/correctness` - architectural truth and contradiction resolution
- `.routines/auditor` - strict quality/security/UX audit source
- `.specs/` - compact durable project contracts
- `.todo/README.md` - planning and status conventions

## Last Ran

- 2026-08-23

## Last Run Remarks

- Reconciled production-readiness implementation into a new compact operations
  card plus backend, frontend, migration, security, and infrastructure specs.
  Stage 2 is complete, its restore/release/load evidence is durable, and
  `linked_external_identities` is the next authoritative sequence member.

- Reconciled the completed commerce-integrity foundation into backend, frontend,
  security, migration, and realtime cards. The durable contract now names
  authenticated hash-only checkout replay, signed provider events, guarded
  transitions, commit-coupled leased fulfilment, atomic payouts, operator
  recovery, migration 039, and replay-suppressed client effects. Stage 1 is
  complete and `production_readiness_and_service_status` is unblocked.

- Documented the `.tip` execution-sequence declaration API and indexed the
  recon-derived platform queue with the `platform_gap_execution_sequence` group
  tag. Index 0 identifies the coordinator; unique contiguous indexes 1-6 are the
  worker order and are now authoritative over prose ordering.

- Created `platform_gap_execution_sequence` as the authoritative stage-gated
  queue for the six recon-derived plans. The order is commerce integrity,
  production readiness, linked identities, external-event rewards, ranked
  datasets, community workflows, then cross-gap audit/retirement. Each linked
  tip now records its stage and prerequisite, and every stage requires stable
  backend policy/tests before shared-primitive UI work.

- Reconciled the public SkaiaCraft recon against current Skaia routes, domains,
  specs, and active plans. Expanded `db_transactions_concurrency` for checkout,
  wallet, callback, payout, special-action idempotency, durable dispatch, leases,
  and database-backed race verification instead of creating overlapping P0 work.

- Created five generic platform handoffs: `production_readiness_and_service_status`,
  `linked_external_identities`, `configurable_ranked_datasets`,
  `external_event_reward_fulfilment`, and
  `community_proposals_showcases_and_events`. They preserve Skaia's tenant,
  privacy, lifecycle, directory, realtime, form, and security contracts and do
  not encode Minecraft-specific routes or entities.

- Prioritization is P0 `db_transactions_concurrency`; P1
  `production_readiness_and_service_status`; then P2
  `linked_external_identities`, `configurable_ranked_datasets`,
  `external_event_reward_fulfilment`, and
  `community_proposals_showcases_and_events`. External-event fulfilment depends
  on the P0 transaction/idempotency foundation. No recon-only P3 polish plan was
  created because Skaia's existing loading, form, media, directory, responsive,
  and accessibility contracts are already stronger and should be audited in
  each affected feature instead of duplicated.

- Retired 21 old pairs. Seventeen implementation plans had complete phases,
  recorded verification, and durable contracts already in specs or enforcement
  checks: audit follow-up, Bible reader, clipmaker reliability, form migration,
  forum mobile layout, guest containment, interactive remediation,
  documentation hub, NAT jail bypass, page-builder responsiveness, resource
  embeds, repository hygiene, section picker, shared media, store orders,
  component registry/refactor, and viewport deferral. The component plan's
  remaining all-route conversion was no longer a desired architecture after
  JSON document consolidation.

- Retired `legacy_data_migration` because its only source tree and old frontend
  path are absent, and retired `project_normalization` because the remaining
  landing names are intentional root-page/config/realtime contracts documented
  by `route_resolution_spec`. Replaced the stale narrow `live_testing` pair with
  the production-readiness plan, preserving its session/Turnstile smoke scope.
  Folded the overlapping Grengo backend-refactor remainder into the normalized
  root-module `grengo_cli_refactor` handoff.

- Kept rollout-gated and incompletely verified plans active, added explicit open
  phases to `critical_fixes_stabilization` and
  `custom_page_document_model_consolidation`, normalized both Grengo plan pairs
  to current root-module paths/status vocabulary, and removed stale references
  to retired todo files from active plans, specs, and routines.

- Created and completed `section_picker_menu_revamp` after confirming the old
  insertion dropdown had incomplete metadata and no scalable discovery surface.
  Reconciled the responsive searchable modal picker and complete 20-type
  metadata contract into the custom-page compatibility spec.

- Reconciled the custom-page presentation contract after section picker labels
  were separated from authored headings for both new and legacy page documents.

- Reconciled and closed `page_section_media_preview_and_resource_embeds` after
  simplifying the page registry to one generic `resource_embed` (20 total
  types), moving lightbox ownership directly into `MediaPlaceholder`, and
  aligning the backend, frontend, custom-page, and documentation contracts.

- Reconciled the completed guest/provisional implementation into the security,
  auth, backend, frontend and realtime contracts: persisted age/TOTP trust,
  closed guest exceptions, aggregate-only analytics, public profile DTOs,
  disabled browser control-plane ingress, DB-backed WS identity/permissions,
  deduplicated views, Redis budgets and optional-TOTP onboarding are authoritative.
- Marked all plan phases complete after the full backend/frontend/race/policy
  verification set and removed remaining stale `/pages/list` references.

- Created `guest_and_provisional_account_containment` from the whole-system
  guest/new-account audit. The handoff prioritizes confirmed unauthenticated
  Grengo control/telemetry, media-history, wallet-credit, teleport, private-page,
  visitor-analytics, public-profile, realtime-identity, and expensive guest-action
  containment before a centralized 30-minute-or-TOTP trust policy,
  deny-before-work registry, distributed tier budgets, and non-coercive onboarding.
  Folded and retired the overlapping unfinished smart-rate-limiter plan after
  preserving its useful weighted-budget, behavior-isolation, telemetry, and admin
  control requirements.

- Reconciled the implemented Phase 2 and intent/data portion of Phase 3 for
  `viewport_deferred_rendering_and_suspense`. Backend, cache, security, document,
  multiplayer, and frontend specs now define bounded content-free summaries,
  authorized no-store previews, omitted-field preservation, abortable cursor
  loading, combined viewport/intent activation, and revision-keyed preview LRU.
- Reconciled completed Phase 3/4 contracts: `SectionFrame` now separates
  reversible animation from one-shot child mounting, preview roots are clipped,
  every section family has bounded config-aware skeleton geometry, activated
  editor children are sticky, and page/JSON-ID keys define teardown.
- Tightened the Phase 4 skeleton contract after visual review: skeletons mirror
  each renderer's hierarchy, and the hero preserves its established responsive
  full-bleed geometry instead of using the generic media-card composition.
- Closed `viewport_deferred_rendering_and_suspense` after the lean Phase 5-7
  pass added route-family shells, migrated named initial loading states, deferred
  analytics/Recharts, enforced the boundary policy, recorded bounded browse
  measurements, and passed the complete verification set.

## Previous Run Remarks (2026-08-17)

- Created `multi_documentation_hub_and_forum_view` after inventorying the
  standalone `integrate/` documentation reference and tracing Skaia's forum,
  TipTap, renderer, route, feature, lifecycle, realtime, cache, and SSR
  contracts. The worker handoff defines an unlimited multi-set `/doc` domain
  with its own handler/service/repositories and a separate `/forum/docs`
  presentation adapter that updates authoritative forum threads without
  copying them into documentation storage.

## Previous Run Remarks (2026-08-16)

- Closed `custom_page_document_model_consolidation` after the worker removed the
  unused normalized implementation, reconciled durable custom-page contracts,
  and preserved the compact registries/shared UI and JSON interactive security.
  Retired the superseded `typed_page_sections_redesign` plan pair on verified
  code/spec/reference evidence; deployment still requires the normal backup and
  migration 035's response/palette stop condition.

## Previous Run Remarks (2026-08-16)

- Created `custom_page_document_model_consolidation` after reconciling the
  completed typed-page plan with code and the local `home` tenant. All 7 active
  pages remain JSON arrays containing heterogeneous configs/IDs, aliases, and
  interactive sections; normalized flags are absent/default-off, while ordinary
  saves still perform shadow work. The handoff preserves the JSON page contract,
  useful registries, interactive security, realtime behavior, and shared UI,
  while retiring typed APIs, generated universal schemas, cutover/backfill
  commands, unconditional projection, and redundant tables behind a normal
  backup plus lightweight flag/response/token guard.
- Tightened the handoff after operator confirmation that none of the production
  backfill/cutover commands were ever run and every production page remains on
  `pages.content`. The worker now performs only a lightweight flag/response/token
  guard and normal backup rather than constructing another migration program.

## Previous Run Remarks (2026-08-16)

- Created `shared_media_placeholder_adoption` after inventorying 63 direct
  image/video/audio elements across 31 production TSX files plus rich-text,
  dynamic-background, live-stream, and canvas paths. The worker handoff hardens
  the new shared primitive before phased page-content, upload/inbox, commerce,
  profile/branding, security, and thumbnail migrations; it preserves explicit
  avatar, bundled-art, MediaStream, canvas, metadata, map-chrome, and third-party
  player exceptions behind an allowlisted regression gate.

## Previous Run Remarks (2026-08-03)

- Created and completed `forum_category_mobile_layout` after tracing the
  real-device category corruption to a non-shrinking toolbar sharing a flex row
  with a title explicitly allowed to shrink to zero. The focused worker handoff
  owns route-scoped responsive layout, removal of the automatic mount shift, and
  regression/quality gates without changing shared forum contracts.
- Created `page_builder_editor_responsive_controls` after diagnosing the dark-mode
  Appearance select arrows and editable rich-text desktop/mobile layout. The
  focused worker handoff reuses the shared Select primitive, preserves nested
  portal interaction, restores TipTap block controls, and aligns mobile editor
  padding with the renderer without duplicating the broader primitive plan.
- Created and completed `repository_hygiene_and_text_policy` after deduplicating
  it from the narrower legacy-code plan. The handoff covered evidence-backed
  cleanup, a root Go checker, tracked-tree remediation, a non-mutating hook, and
  full verification.
- Removed the orphaned soft-delete ledger after confirming its durable contracts
  already exist in specs and its history remains in the matching plan/tip; todo
  pairing, filename, and status-vocabulary checks now pass.

## Previous Run Remarks (2026-08-02)

- Extended `multi_tenant_seo_origin_integrity` rather than creating an
  overlapping todo after the 2026-08-02 auditor/correctness review of commits
  `57f3dcb3` and `c4381994`. The new blocking phase covers transient template and
  database/config failure classification, hot-shipped index refresh,
  `INDEX_FILE_PATH`, correct `503` semantics, authoritative negative caching,
  deterministic handler/cache tests, restoration of the secret-bearing
  `backends/` Docker exclusion, and pre-rollout quality gates.

## Previous Run Remarks (2026-07-30)

- Created `multi_tenant_seo_origin_integrity` after reproducing production
  cross-host SEO contamination across TheWriterCo and CueballCraft. The review
  traced request-origin HTML stored in path-only Redis entries, Cloudflare
  Flexible-style HTTP origin signaling, broad/default nginx host routing, and
  the shared `localhost /` healthcheck. The worker handoff covers containment,
  canonical tenant origins, semantic cache data, invalidation, host/proxy
  hardening, metadata escaping/config correctness, safe image inspection,
  multi-tenant purge, and public recrawl verification.

## Previous Run Remarks (2026-07-30)

- Created `bible_voice_reader_integration` after matching the ignored
  `integrate/` sources byte-for-byte to the workspace-level TheWriterCo
  repository, staging the missing reader/URL/speech dependencies and local page
  artwork, auditing the current Skaia frontend/backend contracts, and tracing
  the verified KJV corpus. The worker handoff defines the internal embedded
  corpus API, dual query/path URL contract, Jotai boundary, UI translation,
  browser narration lifecycle, provenance caveat, and verification gates.

## Previous Run Remarks (2026-07-26)

- Created `soft_delete_and_destructive_prompts` after tracing checked-in tenant
  and control-plane schemas, live database drift, runtime hard deletes, cascades,
  delete routes, realtime projections, and frontend dialog usage. The plan
  classifies restorable, reversible, revocable, immutable, operational,
  orphaned, and file-backed records; it also makes Skaia's shared Prompt and a
  no-native-browser-dialog gate part of deletion acceptance.

## Earlier Run Remarks (2026-07-18)

- Reconciled the two final Appearance UX findings into the completed
  `typed_page_sections_redesign` plan/tip and durable compatibility spec. The plan
  remains complete after worker verification; deployment does not waive its
  production cutover/retirement evidence gates.

## Older Run Remarks (2026-07-17)

- Retired the completed but contradictory interactive-section plan pair,
  preserved its durable JSON-backed architecture in the spec, and created the
  focused `interactive_sections_audit_remediation` worker handoff.

## Next Steps

- Use planner whenever work needs durable planning, todo lifecycle changes, or specs updates.
