# Routine: planner

## Purpose

Planning and durable-context upkeep routine for Skaia. Use it to create, split,
dedupe, update, or retire `.todo/` entries and to keep `.specs/` aligned with
implemented architecture, models, migrations, protocols, frontend contracts, and
operational policies.

## Triggers

- User references `.routines/planner` or asks to plan, strategize, organize, or retire work
- A todo lacks a `.tip` file or has stale status
- Multiple todos overlap or conflict
- Backend/frontend protocol, model, route, migration, WebSocket, cache, auth,
  infrastructure, or dynamic-page behavior changed
- `worker`, `auditor`, or `correctness` finds planning or spec drift

## Inputs

- `.todo/README.md`
- Relevant `.todo/*` and `.todo/*.tip` files
- `.specs/README.md`, `.specs/PROTOCOL.md`, and affected spec cards
- Changed code/tests/migrations when updating specs
- `.routines/correctness` for truth/drift questions
- `.routines/auditor` for quality gates or audit-driven plans
- `.routines/worker` when planning executable implementation steps

## Responsibilities

- Generate, organize, and maintain todos in `.todo/`.
- Ensure every active todo has a compact, current `.tip`.
- Split, merge, dedupe, or retire todos when it improves execution clarity.
- Keep todo entries scoped, actionable, and linked to relevant specs/routines.
- Keep `.specs/` authoritative, compact, and current after contract changes.
- Document durable architecture/model/protocol decisions without pasting full code.
- Route quality/security/UX findings through `.routines/auditor` when the right fix is unclear.
- Escalate contradictions to `.routines/correctness` before encoding them as plans or specs.

## Non-Goals

- Do not implement code changes; hand execution to `.routines/worker`.
- Do not run broad quality audits; use `.routines/auditor`.
- Do not decide contradictory facts by preference; use `.routines/correctness`.
- Do not copy large spec bodies into todos or large code/migration bodies into specs.

## Workflow

1. Identify whether the task is todo planning, spec upkeep, or both.
2. Read the smallest relevant todo/spec/routine set.
3. For todos:
   - Confirm matching `.tip` files exist.
   - Compare plan/status with current project state when needed.
   - Update, split, merge, or retire with documented verification basis.
4. For specs:
   - Read affected spec cards and implementation truth.
   - Patch compact contracts using `.specs/PROTOCOL.md`.
   - Run reference checks for renamed routes/files/messages/tables.
5. Cross-link related specs, todos, and routines.
6. Record durable findings in `.tip`, specs, or planner run remarks.

## Verification

- Confirm every active todo has a matching `.tip`.
- Confirm new plans do not duplicate existing `.todo` entries.
- Confirm `.tip` files follow `.todo/README.md`.
- For retired todos, record verification basis before deletion.
- For specs, use `rg` to confirm stale names/paths/contracts are gone.
- Verify linked files exist.
- For code-backed spec changes, cite the verification already run by `worker` or `auditor`.

## Trace Outputs

- New or updated `.todo/<entry>` files
- New or updated `.todo/<entry>.tip` files
- Updated `.specs/*` files
- Planner run remarks for material planning/spec maintenance

## Contexts

- `.todo/`
- `.todo/*.tip`
- `.todo/README.md`
- `.specs/`
- `.specs/README.md`
- `.specs/PROTOCOL.md`
- `.routines/correctness`
- `.routines/auditor`
- `.routines/worker`
- `main.go`
- `router.go`
- `internal/`
- `backend/`
- `frontend/`

## Extension/See Also

- `.routines/worker` - execution of planned work
- `.routines/correctness` - architectural truth and contradiction resolution
- `.routines/auditor` - strict quality/security/UX audit source
- `.specs/` - compact durable project contracts
- `.todo/README.md` - planning and status conventions

## Last Ran

- 2026-08-03

## Last Run Remarks

- Created `page_builder_editor_responsive_controls` after diagnosing the dark-mode
  Appearance select arrows and editable rich-text desktop/mobile layout. The
  focused worker handoff reuses the shared Select primitive, preserves nested
  portal interaction, restores TipTap block controls, and aligns mobile editor
  padding with the renderer without duplicating the broader primitive plan.
- Created and completed `repository_hygiene_and_text_policy` after deduplicating
  it from the narrower legacy-code plan. The handoff covered evidence-backed
  cleanup, a root Go checker, tracked-tree remediation, a non-mutating hook, and
  full verification.
- Removed the orphaned soft-delete ledger after confirming its durable contracts
  already exist in specs and its history remains in the matching plan/tip; todo
  pairing, filename, and status-vocabulary checks now pass.

## Previous Run Remarks (2026-08-02)

- Extended `multi_tenant_seo_origin_integrity` rather than creating an
  overlapping todo after the 2026-08-02 auditor/correctness review of commits
  `57f3dcb3` and `c4381994`. The new blocking phase covers transient template and
  database/config failure classification, hot-shipped index refresh,
  `INDEX_FILE_PATH`, correct `503` semantics, authoritative negative caching,
  deterministic handler/cache tests, restoration of the secret-bearing
  `backends/` Docker exclusion, and pre-rollout quality gates.

## Previous Run Remarks (2026-07-30)

- Created `multi_tenant_seo_origin_integrity` after reproducing production
  cross-host SEO contamination across TheWriterCo and CueballCraft. The review
  traced request-origin HTML stored in path-only Redis entries, Cloudflare
  Flexible-style HTTP origin signaling, broad/default nginx host routing, and
  the shared `localhost /` healthcheck. The worker handoff covers containment,
  canonical tenant origins, semantic cache data, invalidation, host/proxy
  hardening, metadata escaping/config correctness, safe image inspection,
  multi-tenant purge, and public recrawl verification.

## Previous Run Remarks (2026-07-30)

- Created `bible_voice_reader_integration` after matching the ignored
  `integrate/` sources byte-for-byte to the workspace-level TheWriterCo
  repository, staging the missing reader/URL/speech dependencies and local page
  artwork, auditing the current Skaia frontend/backend contracts, and tracing
  the verified KJV corpus. The worker handoff defines the internal embedded
  corpus API, dual query/path URL contract, Jotai boundary, UI translation,
  browser narration lifecycle, provenance caveat, and verification gates.

## Previous Run Remarks (2026-07-26)

- Created `soft_delete_and_destructive_prompts` after tracing checked-in tenant
  and control-plane schemas, live database drift, runtime hard deletes, cascades,
  delete routes, realtime projections, and frontend dialog usage. The plan
  classifies restorable, reversible, revocable, immutable, operational,
  orphaned, and file-backed records; it also makes Skaia's shared Prompt and a
  no-native-browser-dialog gate part of deletion acceptance.

## Earlier Run Remarks (2026-07-18)

- Reconciled the two final Appearance UX findings into the completed
  `typed_page_sections_redesign` plan/tip and durable compatibility spec. The plan
  remains complete after worker verification; deployment does not waive its
  production cutover/retirement evidence gates.

## Older Run Remarks (2026-07-17)

- Retired the completed but contradictory interactive-section plan pair,
  preserved its durable JSON-backed architecture in the spec, and created the
  focused `interactive_sections_audit_remediation` worker handoff.

## Next Steps

- Use planner whenever work needs durable planning, todo lifecycle changes, or specs updates.
