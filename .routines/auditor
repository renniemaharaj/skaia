# Routine: auditor

## Purpose

Strict whole-repository audit routine for enterprise-grade quality across Skaia:
backend, frontend, dynamic pages, WebSocket protocols, migrations, specs, todos,
routines, security, performance, tests, and user experience. It is more demanding
than `correctness`: `correctness` resolves truth; `auditor` challenges whether the
truth and implementation meet the highest maintainability and product-quality bar.

## Triggers

- User references `.routines/auditor` or asks for an audit/review
- Cross-cutting backend/frontend contracts changed
- A release, major feature, migration, auth/security, store/payment, page-builder, or routine change needs review
- `correctness`, `worker`, or `planner` asks for a quality gate

## Inputs

- `.routines/README.md`, `.routines/correctness`, and the routine being audited
- `.specs/README.md`, `.specs/PROTOCOL.md`, and relevant spec cards
- `.specs/security_architecture_spec` for any security-sensitive scope
- Related `.todo/*` and `.todo/*.tip`
- Changed source/tests/migrations/package manifests
- Existing verification outputs and git diff/status

## Responsibilities

- Audit the entire affected system, not just the changed file.
- Enforce backend boundaries: handler/service/repository, authz, validation, migrations, cache, tests.
- Enforce frontend quality: intentional design, token CSS, accessibility, mobile/empty/error/loading states, real-time state ownership.
- Enforce dynamic-page quality: schema validation, page-builder edit safety, section reactivity, no stale CDN/data flows.
- Review security and privacy first: auth, MFA, sessions, permissions, uploads, SSRF/XSS/CSS injection, payment/card data exposure.
- Reject sensitive operations that bypass centralized service policy, fresh
  action/session-bound step-up, fail-closed decisions, or required audit trails.
- Review performance and reliability: N+1s, unbounded memory/goroutines, polling, cache invalidation, slow-client WS behavior.
- Audit docs/routines/specs/todos for stale references, duplicated authority, and poor handoff quality.
- Produce findings ordered by severity with file/line evidence when possible.
- Fix small safe documentation/process drift in scope; send implementation work to `worker` or `.todo` when larger.

## Non-Goals

- Do not replace `correctness` as the final arbiter of contradictory facts.
- Do not implement broad fixes without a scoped todo or user request.
- Do not praise style without evidence; findings must be actionable.
- Do not lower the bar because code is legacy; name legacy risk clearly.

## Workflow

1. Define audit scope and blast radius.
2. Read the relevant specs, routines, todos, and implementation files.
3. Run `correctness` mentally/procedurally against the same scope: specs and code must agree.
4. Inspect quality dimensions: security, data model, API, UI, state, realtime, tests, docs, operations.
5. Classify findings: Critical, High, Medium, Low, or No material issue.
6. Apply safe doc/routine/spec corrections directly; otherwise create/update todo guidance.
7. Verify references and run tests/builds proportional to changes.
8. Record durable audit facts in `.tip`, specs, or auditor run remarks.

## Verification

- Docs/routine-only: `rg` stale names, verify linked files exist, inspect `git diff --stat`.
- Backend changes: `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...`.
- Frontend changes: from `frontend/`, run the relevant build/test/lint command.
- Migrations: verify sorted filenames and idempotency patterns; note DB-backed gaps.
- UI changes: inspect relevant CSS for token compliance and responsive/empty/error states.

## Trace Outputs

- Findings in the final response or related `.todo/<entry>.tip`
- Updated specs via `.routines/planner` when contracts changed
- Updated routine run remarks for material standalone audits

## Contexts

- `.routines/`
- `.specs/`
- `.todo/`
- `main.go`
- `router.go`
- `internal/`
- `backend/`
- `frontend/`

## Extension/See Also

- `.routines/correctness` - architectural truth and contradiction resolution
- `.routines/worker` - scoped implementation fixes
- `.routines/planner` - durable plan creation/splitting/retirement and compact spec upkeep

## Last Ran

- 2026-08-20

## Last Run Remarks

- Audited direct `MediaPlaceholder` preview activation and the generic resource
  embed across viewer/editor gallery overlay hit-testing, nested preview ownership, source
  visibility, picker bounds, realtime teardown, loading/error/empty states, and
  responsive/token CSS. No Critical, High, or Medium issue remains after the
  complete backend/frontend verification set.

- Completed the affected-system containment audit after implementation. Guest
  access is browse-only by default with closed low-cost exceptions; provisional
  accounts cannot reach durable or expensive actions; Grengo/media/teleport,
  wallet, pages, analytics, datasource env, public profile and realtime identity
  findings are contained. No Critical, High or Medium issue remains in scope.
- The complete backend suite, WS/security race tests, all 213 frontend tests,
  lint, TypeScript, native-dialog policy, production build, reference and diff
  gates pass. Deterministic tests cover the browser matrix because browser
  automation was unavailable in this environment.

- Audited guest and newly registered access across HTTP, WebSocket, auth/TOTP,
  forum, pages/interactive records, inbox, uploads, store/wallet, datasource
  execution, voice, analytics, rate limiting, audit, schemas, specs, and tests.
  Confirmed critical unauthenticated Grengo control-plane action forwarding,
  individual media-history deletion, and unrestricted authenticated wallet
  credit, plus globally broadcast infrastructure telemetry, permissionless member
  teleport, unrestricted visitor IP/identity analytics, private full-page listing,
  spoofable realtime identity, ungoverned guest expensive actions, and absence of
  any account-age/TOTP mutation tier.
  Recorded the implementation handoff in
  `.todo/guest_and_provisional_account_containment`; no application code changed.

- Audited `viewport_deferred_rendering_and_suspense` across browse/preview
  authorization and bounds, section mount ownership, preview/view renderer
  parity, route and primary-surface loading states, analytics deferral, action
  availability, responsive/token/reduced-motion CSS, tests, policies, specs, and
  todo completion. No Critical, High, or Medium issue remains; complete backend
  tests, all 210 frontend tests, lint, build, four policy gates, and diff checks
  pass. The 500-record fixture produces 100 summaries/19,392 bytes with one
  repository call and no document content.
- Final commit-handoff polish traced each page skeleton against its renderer DOM
  and CSS, removing invented CTA/rich-text/social content, restoring profile
  hierarchy, matching flat collection chrome, and correcting root/form/wallet
  route shells. No material finding remains after the complete gate rerun.
- Audited shared-media Phases 1 and 2 across stale URL events, empty/error/loading
  accessibility, fixed geometry, reduced motion, theme tokens, hero cycling,
  picker overlays, profile-avatar semantics, sanitization, and saved rich-text
  stability. No Critical, High, or Medium issue remains in the implemented scope.
- The exact-count media gate, 178 frontend tests, lint, TypeScript, production
  build, and diff checks pass. Manual throttling and cross-viewport/theme checks
  remain a Phase 6 completion gate after all consumer migrations land.
- Audited Phase 3/4 failure states against preview close/cycle, file download,
  product navigation, add-to-cart, quantity/removal, and media deletion controls.
  The corrected modal restores focus and handles Escape/arrow keys; all 178 tests,
  lint, TypeScript, build, enforcement, and diff gates pass.
- Completed the final affected-scope media audit across accessibility, decorative
  media, theme tokens, reduced motion, responsive fixed frames, URL replacement,
  background URL safety, playback, modal focus/keyboard behavior, and failure-safe
  parent actions. No Critical, High, or Medium finding remains. All 181 tests,
  lint, TypeScript, production build, both policy gates, and diff checks pass;
  real-browser throttling was unavailable and is covered by deterministic
  lifecycle/error tests in this environment.

## Previous Run Remarks (2026-08-03)

- Audited repository bloat, tracked text, all Go modules, frontend dependencies,
  Husky behavior, todo contracts, generated contracts, and fingerprinted corpus
  data. Removed 76 MB of build outputs, unreachable and commented code, empty
  artifacts, unused packages, and redundant planning material.
- No material finding remains in scope. Root/backend tests, vet, staticcheck, all
  six Go module suites, all 152 frontend tests, lint, build, and policy gates pass.

## Previous Run Remarks (2026-07-26)

- Audited `multi_tenant_seo_origin_integrity` across tenant origin trust,
  request-order/auth independence, Redis keying and invalidation, HTML escaping,
  soft-404/indexing policy, SSRF/latency, generated nginx, health traffic,
  control-plane env lifecycle, sitemap alignment, tests, specs, and rollout.
- The final pass added static page alias/old-slug invalidation, made mutable live
  stream metadata uncacheable, kept tenant-matched health checks local, surfaced
  invalidation failures, added a generated two-tenant fixture, and repaired one
  pre-existing root static-analysis defect encountered by the gate.
- No Critical, High, or Medium implementation issue remains in the affected
  code. Full backend/root tests and vet, affected race/static analysis, builds,
  and diff checks pass. The live sites still expose the old defect and require
  deployment plus Cloudflare/tenant inventory before recrawl.

## Previous Run Remarks (2026-07-26)

- Audited the WebSocket ingress, subscription registry, REST rerouter,
  connection lifecycle, recovery correlation, private order/user projections,
  protobuf contract, and frontend consumers. No Critical, High, or Medium issue
  remains in the affected scope.
- During the gate, added global REST-bridge concurrency, request/response header
  bounds, subscription deduplication/caps, stale-client rejection, presence-field
  synchronization, token-change reconnects, and adversarial regression coverage.
- Final verification passed: full backend suite; `go test -race ./internal/ws
  ./internal/page`; all 124 frontend tests; frontend lint/build; contract drift;
  and `git diff --check`.

## Previous Run Remarks (2026-07-18)

- Completed the final affected-scope production audit for
  `typed_page_sections_redesign`: no Critical, High, or Medium issue remains in
  the implementation. Restored the exact contract-generation gate and removed
  four stale false-negative frontend assertions. Code deployment is green;
  normalized reads and legacy retirement remain guarded by real production
  parity, rollback, quiet-window, and external-dependency evidence.

## Previous Run Remarks (2026-07-17)

- Audited `b3867355..a33fc6c9` across JSON persistence, HTTP/WS privacy,
  authorization, idempotency, validation, copy/duplicate safety, realtime state,
  Formik UI, tests, CSS, and todo/spec drift; recorded prioritized remediation in
  `.todo/interactive_sections_audit_remediation`.

## Next Steps

- Use this routine for future review/audit gates and retire old audit redirects after dependent references are updated.
