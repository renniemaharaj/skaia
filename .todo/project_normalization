Project-wide Normalization: 'landing' vs 'page'

This plan tracks the renaming of all 'landing' references to 'page' across the codebase.

## Status
- Blocked on manual spec-file renames and broad codebase audit.
- Spec drift: `backend_spec` still references `landing_sections` and `landing_items`, but migration 007 renamed them to `page_sections` and `page_items`. This must be corrected in the spec.
- `ui_component_refactor_plan` still uses `LandingSection` and `LandingItem` type names.
