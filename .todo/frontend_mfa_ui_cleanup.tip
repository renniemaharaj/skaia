<--/------>
Phase 1: Refactor Auth.tsx TOTP UI
- Status: ⬜ Not started
- Notes: Replace the inline TOTP code inside `Auth.tsx` with the `<MFAChallenge />` component. Ensure props (`totpToken`, etc.) are passed correctly for the login flow.

Phase 2: Cleanup Dead Code in MFAChallenge
- Status: ⬜ Not started
- Notes: Remove unused/dead paths (like `useNavigate`) in `MFAChallenge.tsx` if they become obsolete, or ensure they correctly support both login and mid-session flows.

Phase 3: Remove usePageData Listener
- Status: ⬜ Not started
- Notes: Delete or rework the `auth:mfa-required` listener in `usePageData.ts` (lines 282-292) to prevent redundant 401s during the MFA challenge.
<--/------>
