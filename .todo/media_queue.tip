<--/------->

## media_queue

### Current Progress
- **Backend state and events** — ✅ Complete
- **Frontend sync and player** — ✅ Complete
- **Voice panel integration** — ✅ Complete
- **Correctness hardening** — ✅ Complete
- **Production verification** — ✅ Complete

### Findings Secured
- Correctness finding: media actions trusted `payload.route`; patched `backend/internal/ws/hub_media.go` so actions are ignored unless the requested route matches the connected client's current route.
- Regression coverage: added `TestHandleMediaUpdateRejectsCrossRouteActions` in `backend/internal/ws/hub_media_test.go`.
- Todo drift: normalized `.todo/media-queue.todo` into `.todo/media_queue` plus this `.tip`.

### Verification
- ✅ Complete: `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...`
- ✅ Complete: `mise exec node@24.16.0 -- npm run build`

### Next Steps
- No unchecked implementation or verification items remain.

<--/------->
