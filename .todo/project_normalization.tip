<--/------>
Phase 1: Remove LandingSkeleton
- Status: ✅ Complete
- Notes: Component removed.

Phase 2: Spec Alignment
- Status: ⬜ Not started
- Notes: Need to update `backend_spec` to rename `landing_sections` and `landing_items` to `page_sections` and `page_items`. Rename `.specs/landing_page_spec` to `route_resolution_spec` if not fully resolved.

Phase 3: Codebase Refactor
- Status: ⬜ Not started
- Notes: Update all struct names, DB query variables, and comments from landing to page.

Phase 4: UI Refactor Alignment
- Status: ⬜ Not started
- Notes: Ensure `ui_component_refactor_plan` types use `PageSection`/`PageItem` instead of Landing equivalents.
<--/------>
