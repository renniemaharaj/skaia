<--/------->
## production_readiness_and_service_status
@todo-sequence(group=platform_gap_execution_sequence, index=2)

### Current Progress
- **Phase 1: Health, telemetry, and backup inventory** - ✅ Complete
- **Phase 2: Readiness and protected diagnostics** - ✅ Complete
- **Phase 3: Public status and incidents** - ✅ Complete
- **Phase 4: Restore and retention verification** - ✅ Complete
- **Phase 5: Release and load gates** - ✅ Complete
- **Phase 6: Operator/public UX and specs** - ✅ Complete

### Completed Work
- [x] Kept `/health` dependency-free and added bounded parallel database,
  schema, Redis, and SPA readiness checks. Public probes expose generic state;
  live `admin.general` policy protects latency, reason, pool, and queue detail.
- [x] Added opt-in public status, audited incident/maintenance lifecycle,
  immutable event history, delayed-data fallback, canonical `/status` SEO, and
  shared-primitive public/operator UX across all required states.
- [x] Added mode-0600 backup manifests, integrity verification, declared
  retention/encryption requirements, safe archive inspection, and an isolated
  restore runbook.
- [x] Added machine-readable release verification and capped loopback-first HTTP
  and WebSocket load samples. Session/Turnstile, store, sitemap, SPA, migration,
  configuration, liveness, and readiness checks now fail closed.
- [x] Reconciled backend, frontend, migration, security, infrastructure, and the
  new operations-readiness specification and runbook.

### Verification Evidence (2026-08-23)
- [x] Full backend tests and vet passed; focused status/security/session/SEO race
  tests passed. Failure tests cover SQL, Redis, schema, filesystem, timeout,
  stale fallback, policy failure, and public diagnostic non-disclosure.
- [x] Root application/CLI tests and vet passed. Backup verification returned
  `proceed` for SHA-256 `39a16917d949be30b1992fca45a3cdc503a24998136e7d72418fa982b393b858`.
- [x] All 238 frontend tests, lint, managed-form/native-dialog/loading/media
  policy checks, TypeScript, and production build passed.
- [x] A named disposable database and backend restored the retained migration
  backup, applied migration 040, served all four `/ready` components as
  operational, matched 2 users / 7 pages / 288 events / 1 product, and rejected
  direct non-superuser incident-event deletion. Its database, role, and
  container were removed after evidence capture.
- [x] Final local release verification returned `proceed`. HTTP readiness ran
  40 requests at concurrency 4 with zero failures and 1 ms average latency;
  WebSocket reconnect ran 10 attempts at concurrency 2 with zero failures and
  1 ms average latency, both under the 500 ms budget.

### Audit and Correctness
- [x] Public readiness/status no longer execute operator-only pool/queue queries.
  The WebSocket sampler supplies the required same-origin header and has a
  regression test. No material affected-code/spec contradiction remains.
- [ ] Deployment follow-up remains in `soft_delete_and_destructive_prompts`:
  legacy local tenants connect with the PostgreSQL bootstrap superuser. Real
  environments must use a separate non-superuser application login for database
  hard-delete guards to provide defense in depth.

### Next Steps
- [x] 2026-08-23: `db_transactions_concurrency` completed its integrity and
  verification gates; execution stage 2 is unblocked.
- [x] Completed the probe/dependency/telemetry/backup matrix and durable public
  versus operator boundaries.
- [x] Folded session/Turnstile and representative service smoke checks into the
  release verifier.
- [ ] Begin sequence Stage 3, `linked_external_identities`; do not expand status
  contracts with provider-specific identity behavior.

<--/------->
