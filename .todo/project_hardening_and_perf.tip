<--/------>
Phase 1: Database Indexes
- Status: ✅ Complete
- Notes: Created a new migration (010_add_indexes.sql) to add missing indexes for `auth_backup_codes(user_id)` and `page_comment_likes`.

Phase 2: Redundant DB Queries & Logic
- Status: ✅ Complete
- Notes: Removed duplicate `GetTOTPEnabled` check in Login handler. Documented double JWT parsing in MFA middleware.

Phase 3: Dead Code Removal
- Status: ✅ Complete
- Notes: Deleted `totp.go` (hand-rolled impl). Removed `TOTPBackupCode` from `models/auth.go`. Removed `LoginRequest.TOTPCode`. Cleaned up `authhandler.Service`.

Phase 4: Repository Cleanup
- Status: ✅ Complete
- Notes: Removed `SetTOTPSecret` and replaced with `CreateTOTPSecret` everywhere.
<--/------>
