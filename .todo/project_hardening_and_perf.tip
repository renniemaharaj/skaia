<--/------>
Phase 1: Database Indexes
- Status: ⬜ Not started
- Notes: Create a new migration to add missing indexes for `auth_backup_codes(user_id)` and `page_comment_likes`.

Phase 2: Redundant DB Queries & Logic
- Status: ⬜ Not started
- Notes: Remove duplicate `GetTOTPEnabled` check in Login handler. Address double JWT parsing in MFA middleware (either reorder or document).

Phase 3: Dead Code Removal
- Status: ⬜ Not started
- Notes: Delete `totp.go` (hand-rolled impl). Remove `TOTPBackupCode` from `models/auth.go`. Remove `LoginRequest.TOTPCode`. Clean up `authhandler.Service`.

Phase 4: Repository Cleanup
- Status: ⬜ Not started
- Notes: Consolidate `SetTOTPSecret` and `CreateTOTPSecret` into a single unified method in `repository.go`.
<--/------>
