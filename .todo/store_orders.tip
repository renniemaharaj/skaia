<--/------->

## store_orders

### Current Progress
- **Phase 1: Backend Models & Schema** — ✅ Complete
- **Phase 2: Backend Checkout & Lookup APIs** — ✅ Complete
- **Phase 3: Backend Admin APIs** — ✅ Complete
- **Phase 4: Frontend Cart & Checkout UI** — ✅ Complete
- **Phase 5: Frontend Lookup & Admin UI** — ✅ Complete

### Next Steps
- [x] 2026-06-16 regression hotfix: authenticated cart quantity edits now persist via `/store/cart/update`.
- [x] 2026-06-16 UI stabilization: cart summary sizing, saved checkout card styles, store mobile category bar padding, and image lightbox styles moved/tightened in CSS.
- [x] 2026-06-16 refactor cleanup: removed dead/commented cart button code, reduced inline style fragments, typed saved card data, and simplified delivery marker parsing.
- [x] Verified with `mise exec node@24.16.0 -- npx biome lint src/pages/cart/index.tsx src/styles/Cart.css src/components/store/Store.tsx src/components/store/Store.css` from `backend/frontend`.
- [x] Verified with `mise exec node@24.16.0 -- npm run build` from `backend/frontend`.
- [x] 2026-06-18 store polish: added product-owner filtering with glass `PersonPicker`, `You` owner toggle, dynamic wallet balance and pending-order badges, restored readonly location map preview from order coordinates, and added seller order notifications plus system inbox messages.
- [x] Updated `.specs/realtime_wss_spec`, `.specs/backend_spec`, and `.specs/frontend_spec` for order update/notification and store filter badge contracts.
- [x] Verified with `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...` from `backend`.
- [x] Verified with `mise exec node@24.16.0 -- npx biome lint src/components/store/Store.tsx src/components/store/StoreCategoryBar.tsx src/components/store/Store.css src/components/store/OrdersPage.tsx src/components/store/OrdersPage.css src/atoms/notifications.ts` from `backend/frontend`.
- [x] Verified with `mise exec node@24.16.0 -- npm run build` from `backend/frontend`.
- [x] 2026-06-18 follow-up: compacted wallet balance thousands, counted pending vendor line work, rendered all colored rich-user roles in global messages, added vendor-scoped order accept/reject with aggregate buyer tracking, consolidated `store-seller` into `Vendor`, and fixed recovery approval sessions from being cleared by generic unauthorized/guest teardown handling.
- [x] Added repository coverage for vendor-scoped order status and stock isolation.
- [x] Verified follow-up with `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...` from `backend`.
- [x] Verified follow-up with `mise exec node@24.16.0 -- npx biome lint src/atoms/store.ts src/components/store/Store.tsx src/components/store/StoreCategoryBar.tsx src/components/store/OrderStatusView.tsx src/components/store/OrdersPage.tsx src/components/store/OrderViewPage.tsx src/components/user/UserInlineCard.tsx src/components/user/UserInlineCard.css src/styles/Cart.css src/pages/Layout.tsx` from `backend/frontend` (warnings remain in existing `Layout.tsx` rate-limit/dependency code).
- [x] Verified follow-up with `mise exec node@24.16.0 -- npm run build` from `backend/frontend`.
- [x] 2026-06-18 realtime/filter polish: made store price min/max resilient to inverted bounds and flex-wrapped the filter panel, propagated order updates to `order:{id}` subscribers, subscribed rendered/user orders for live cards and badges, rendered order JSON inbox messages as compact route cards, replaced the empty-cart CTA card with a compact "No products added" state, and added colored join/leave arrows.
- [x] Verified realtime/filter polish with `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...` from `backend`.
- [x] Verified realtime/filter polish with `mise exec node@24.16.0 -- npx biome lint src/components/store/Store.tsx src/components/store/Store.css src/components/store/OrderStatusView.tsx src/components/store/cart/EmptyCart.tsx src/styles/Cart.css` from `backend/frontend`.
- [x] `mise exec node@24.16.0 -- npx biome lint src/components/inbox/InboxPage.tsx src/components/page/layout/PresencePanel.tsx ...` still reports pre-existing whole-file lint debt (`any`, missing button types, `forEach`, etc.); TypeScript/build verification passed.
- [x] Verified realtime/filter polish with `mise exec node@24.16.0 -- npm run build` from `backend/frontend`.
- [x] 2026-06-18 DEFCON tab polish: added compact reset control, admin-only `/defcon/reset`, synchronous telemetry refresh, and telemetry broadcast.
- [x] Verified DEFCON reset with `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...` from `backend` and `mise exec node@24.16.0 -- npm run build` from `backend/frontend`.
- [x] 2026-06-18 DEFCON/rich-user hotfix: DEFCON reset now preserves trusted citizens and promotes the resetting admin IP; priority bypass endpoint now verifies MFA and grants bypass/trusted access directly; rich user cards/profile overlays dedupe in-flight `/users/{id}` and `/users/roles` requests.
- [x] Verified DEFCON/rich-user hotfix with `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...` from `backend` and `mise exec node@24.16.0 -- npm run build` from `backend/frontend`.
- [x] 2026-06-18 production audit correction: Priority Access UI now appears only when the backend returns `challenge:"totp"`, stale challenge/telemetry session state is cleared on non-eligible 429s, retry timers consume JSON `retry_after`, and profile overlays no longer eager-fetch every mounted user card.
- [x] Verified audit correction with `mise exec node@24.16.0 -- npx vitest run src/pages/RateLimitedPage.test.tsx`, `mise exec node@24.16.0 -- npx tsc -b --pretty false`, focused `biome lint` (warnings only), `env GOCACHE=/tmp/skaia-go-cache GOTMPDIR=/tmp JWT_SECRET=test-secret go test ./...` from `backend`, and the same Go test from `grengo`.

<--/------->
