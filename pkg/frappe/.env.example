# Demo Instance - Cost Optimized Configuration
# For t3.micro AWS EC2 instance with 3 heavy users

# SSH Key-Based Authentication
# IMPORTANT: Set your SSH public key here for secure key-based access
# Get your public key: cat ~/.ssh/id_rsa.pub
# Then: export SSH_PUBLIC_KEY="$(cat ~/.ssh/id_rsa.pub)"
SSH_PUBLIC_KEY=

# Database
MARIADB_HOST=mariadb
MARIADB_PORT=3306
MARIADB_ROOT_PASSWORD=root
MARIADB_ROOT_USERNAME=root
MARIADB_USER=frappe
MARIADB_PASSWORD=frappe
MARIADB_DATABASE=frappe

# Frappe Branch (cost optimization: use stable branch)
FRAPPE_BRANCH=develop

# Redis (using default localhost)
REDIS_CACHE_URL=redis://redis-cache:6379
REDIS_QUEUE_URL=redis://redis-queue:6379
REDIS_SOCKETIO_URL=redis://redis-socketio:6379
