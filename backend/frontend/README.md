# Frontend

React 19 + TypeScript + Vite SPA.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Production build is served by nginx (see `nginx/`).

## Lint / Format

```bash
npm run lint
npm run format
```

Uses Biome for linting and formatting.
