# Auto-generated by grengo – 2026-06-19T14:28:14-04:00
# Do not edit manually; run 'grengo nginx reload' to regenerate.

# ── gzip ───────────────────────────────────────────────────────────────────
gzip              on;
gzip_comp_level   5;
gzip_min_length   256;
gzip_vary         on;
gzip_proxied      any;
gzip_types
    text/plain text/css text/javascript text/xml
    application/javascript application/json
    application/xml application/xml+rss application/wasm
    image/svg+xml font/woff2;

# ── Upstreams ──────────────────────────────────────────────────────────────
upstream home-backend {
    server 127.0.0.1:1080;
    keepalive 32;
}

# ── Host => backend mapping ────────────────────────────────────────────────
map $host $backend_upstream {
    localhost               home-backend;
    default                 home-backend;
}

# ── WebSocket upgrade ─────────────────────────────────────────────────────
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# ── Upload cache ──────────────────────────────────────────────────────────
proxy_cache_path /var/cache/nginx/uploads
                 levels=1:2 keys_zone=uploads_cache:10m
                 max_size=1g inactive=30d use_temp_path=off;

server {
    listen 80;
    server_name localhost;
    client_max_body_size 0;
    proxy_intercept_errors on;

    error_page 502 503 504 =503 /fallback-503;
    location = /fallback-503 {
        internal;
        default_type text/html;
        return 503 "<!doctype html>\n<html lang=\"en\">\n\n<head>\n  <meta charset=\"UTF-8\" />\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n  <!-- Server-injected SEO placeholders -->\n  <title>Starting...</title>\n  <meta name=\"description\" content=\"Service is currently restarting. Please wait a moment.\">\n  \n  <link rel=\"icon\" href=\"/logo.png\">\n  <script type=\"module\" crossorigin src=\"/assets/index-CtH16l_G.js\"></script>\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/react-DtrESx-C.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/preload-helper-DzJ41m7t.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/vanilla-CxoMEv8F.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/react-06ROAcon.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/utils-B6M7pNB0.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/createLucideIcon-DprWAZ2L.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/chevron-down-Djv8AKEh.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/chevron-left-Bs2uSfgw.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/circle-alert-w7KcBCBU.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/jsx-runtime-DZxhjIxK.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/UserAvatar-GN-ozPSp.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/x-CLnJEsbn.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/api-DIbhsh4a.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/chunk-LFPYN7LY-ClK2w_L1.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/auth-DcFlbHoX.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/guestSandbox-pXVseu73.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/ContentFlatCard-e1G_C8lq.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/Button-D5nPzXLd.js\">\n  <link rel=\"modulepreload\" crossorigin href=\"/assets/Prompt-CFRHTVnG.js\">\n  <link rel=\"stylesheet\" crossorigin href=\"/assets/UserAvatar-CscvbAmD.css\">\n  <link rel=\"stylesheet\" crossorigin href=\"/assets/ContentFlatCard-CAVR5gbX.css\">\n  <link rel=\"stylesheet\" crossorigin href=\"/assets/Button-B3iM4nd6.css\">\n  <link rel=\"stylesheet\" crossorigin href=\"/assets/index-C8Pegzxo.css\">\n  <meta http-equiv=\"refresh\" content=\"2\">\n</head>\n\n<body>\n  <div id=\"root\"></div>\n</body>\n\n</html>";
    }

    # Security headers
    add_header X-Frame-Options        "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy     "camera=(), microphone=(self), geolocation=()" always;

    # ── Uploads (cached) ──────────────────────────────────────────────────
    location ^~ /uploads/ {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";

        proxy_cache           uploads_cache;
        proxy_cache_valid     200 30d;
        proxy_cache_valid     404 1m;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_lock      on;
        add_header X-Cache-Status $upstream_cache_status;
        expires 30d;
        add_header Cache-Control "public";
        add_header X-Content-Type-Options "nosniff" always;
        access_log off;
    }

    # ── API ───────────────────────────────────────────────────────────────
    location /api/ {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
        proxy_buffering    on;
        proxy_buffer_size  16k;
        proxy_buffers      8 16k;

        # Chunked upload completion can take minutes for large files
        proxy_read_timeout 3600;
        proxy_send_timeout 3600;

        # Prevent CDN / browser caching of API responses
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma        "no-cache" always;
    }

    # ── Downloads (No Buffering) ──────────────────────────────────────────
    location ~ ^/api/s/.*/(?:exports|jobs)/.*/download$ {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
        proxy_buffering    off;
        
        # Prevent timeouts during long downloads
        proxy_read_timeout 3600;
        proxy_send_timeout 3600;
    }

    # ── Config / page endpoints ───────────────────────────────────────────
    location /config/ {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";

        # Prevent CDN / browser caching of config responses
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma        "no-cache" always;
    }

    # ── WebSocket ─────────────────────────────────────────────────────────
    location = /ws {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        $connection_upgrade;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    location = /api/ws {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        $connection_upgrade;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Webhook ───────────────────────────────────────────────────────────
    location = /webhook/github {
        proxy_pass         http://127.0.0.1:9100;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # ── Health ────────────────────────────────────────────────────────────
    location = /health {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host        $host;
        proxy_set_header   Connection  "";
    }

    # ── Catch-all => SSR / SPA ────────────────────────────────────────────
    location / {
        proxy_pass         http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
        proxy_buffering    off;
        proxy_buffer_size  16k;
        proxy_buffers      8 16k;
    }
}
