# Auto-generated by grengo
services:
  backend:
    image: skaia-backend:latest
    container_name: ${CLIENT_NAME}-backend
    restart: unless-stopped
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
    networks:
      - skaia-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${PORT}/health"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  skaia-network:
    external: true
